{
  "name": "newweb-app",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev -- --host 0.0.0.0",
    "build": "npm --workspace frontend run build",
    "preview": "npm --workspace frontend run preview -- --host 0.0.0.0",
    "lint": "npm --workspace frontend run lint"
  }
}